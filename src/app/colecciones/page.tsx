import CollectionsPage from '@/components/coleccionesPage/coleccionesPage';

export default function ColeccionesPage() {
    return (
        <main className="w-full overflow-x-hidden">
            <CollectionsPage />
        </main>
    );
}
